name: System Monitor & Auto-Backup

on:
  schedule:
    - cron: "0 0 * * *" # Runs every day at midnight UTC
  push:
    branches:
      - main

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Run Cleanup Script
        run: python3 cleanup_files.py
        working-directory: ${{ github.workspace }}


      - name: Configure Git
        run: |
          git config --global user.email "jamaalroby@gmail.com"
          git config --global user.name "jtroby"
          git config --global credential.helper store
          git remote set-url origin https://x-access-token:${{ secrets.GH_TOKEN }}@github.com/jtroby/devika.git

      - name: Commit System State
        run: |
          git add ~/automation_logs/ ~/control_center/
          git commit -m "🔄 Auto-Backup System State - $(date)" || echo "No changes to commit"
          git push origin main || echo "No changes to push"

      - name: Notify Discord (Optional)
        run: |
          curl -H "Content-Type: application/json" \
          -X POST -d '{"content": "🔄 System backup completed successfully."}' \
          https://discord.com/api/webhooks/YOUR_WEBHOOK_URL
